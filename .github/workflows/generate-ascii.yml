name: My Second Workflow
on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4

            - name: list repo files
              run: ls -ltra

            - name: Executing shell sctipt
              run: ./ascii-script.sh

